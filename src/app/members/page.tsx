import type { Metadata } from "next";import { Suspense } from "react";import { PageHero } from "@/components/PageHero";import { MemberDirectory } from "@/components/MemberDirectory";import { getMembers } from "@/data/members";
export const metadata:Metadata={title:"Members",description:"Meet the interdisciplinary members of KTP at Ohio State."};
export default async function Members(){const all=await getMembers();return <main id="main"><PageHero kicker="Our people" title="Different disciplines." accent="Shared momentum." body="Engineers, designers, analysts, marketers, and builders of every kind—connected by curiosity and care."/><section className="section"><div className="wrap"><Suspense fallback={<p className="results">Loading roster…</p>}><MemberDirectory members={all}/></Suspense></div></section></main>}
